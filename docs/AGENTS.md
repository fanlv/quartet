## 目录说明

当前目录是用于记录所有 feature/重构的文档目录。新建文档的时候有以下几点需要注意：

1. 单文件文档命名应为 `feature-YYYY-MM-DD-xxx.md`，比如 `feature-2026-05-03-user-input-logging.md`。
2. 多部分设计文档可以使用子目录组织，目录名应有明确语义（如 `multi-user/`），子文件按编号排序（如 `00-overview.md`、`01-auth.md`）。
3. 方案文档里面不要有代码细节，是功能的描述文。
