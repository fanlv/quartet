# Oncall 群聊总结

## Fornax P2：agent run failed，read_file 工具调用失败导致流程中断

这个群讨论的是一次 Agent 运行失败问题。问题发起人朱瑞斌反馈，Agent 在调用内置 `read_file` 工具时读取了不存在的文件 `/workspace/commerce-ai-knowledge/L1-domain/refund/agent-index.md`，返回 `No such file or directory`，随后整个 Agent 流程直接失败。他的预期不是让流程立刻终止，而是把这类工具错误返回给模型，让模型根据错误信息自动纠正路径或重试。

最初 Next 智能助手给出的方向是通过 ADK 中间件把工具执行错误转换成字符串结果返回给模型，而不是作为 error 向外抛出。这样模型能看到类似工具错误提示后继续调整参数。朱瑞斌随后说明自己已经挂了 middleware，但看起来没有生效。马荣鸿追问“没生效”是否指错误没有被 middleware 捕获而是直接透出。朱瑞斌补充说，这次报错只是模型幻觉导致读了不存在路径，他希望模型能自动纠正，而不是整个流程失败。

庄杰先建议如果工具返回 error 会被中间件吞掉，可以不要返回 error，而是直接返回字符串描述。随后朱瑞斌说明这个工具不是他自己写的，而是内置的 `read_file` 工具。庄杰看了截图后判断它实际看起来已经返回了 string，并向朱瑞斌要 trace 进一步排查。朱瑞斌表示找不到 logid，问题也不是必现，重试后没有再次触发。

后续讨论集中在是否为版本问题。庄杰怀疑这个报错发生时可能还在较低版本，当时还没有相关中间件能力，建议朱瑞斌再观察，并提到旁边应该可以看 trace。群内没有形成确定根因，也没有看到最终修复结论。之后 oncall 机器人多次提醒群聊长时间未响应，需要确认是否解决或保存会话。朱瑞斌尝试 `/save`，但系统提示只有租户管理员或值班人员可以保存长期群聊，因此保存失败。

整体来看，这个群的结论是：问题现象为内置 `read_file` 工具因文件不存在报错后导致 Agent 运行整体中断；期望行为是将普通工具错误作为模型可见信息返回，让模型自行纠错；已有方向是检查 ADK middleware 是否真正覆盖到内置工具调用链，以及确认问题发生时运行版本是否包含该能力。由于问题未能稳定复现、logid 缺失，群内没有完成闭环定位。

## Fornax P2：workflow agent 工具调用入参解析异常

这个群讨论的是 workflow agent 是否支持工具调用，以及工具入参为什么没有正确解析到本地工具的请求结构体中。问题发起人王子健反馈，workflow agent 加入工具后，工具调用参数看起来已经生成了 arguments，但没有成功序列化成工具入参 `req`，调试时看到 `req` 的所有参数都是空的。

NextOncall 最初回复说 Workflow Agent 支持工具调用，包括 SequentialAgent、ParallelAgent 和 LoopAgent，并建议检查工具入参 schema、ToolsNode 节点参数和 Eino 版本。王子健转人工后贴了本地工具、初始化代码和调试截图。马荣鸿追问“不能正常解析工具”具体是什么意思，以及泛型类型 `T` 是什么。王德政随后参与排查，指出从调试看传入类型像是 `*uint8`，不是王子健预期的工具入参类型，并询问是否能复现，建议语音调试。

为了排查模型实际发出的工具调用请求，王德政转发了一段用于打印 HTTP 请求为 curl 的 RoundTripper 中间件，建议把真实请求打出来看。王子健随后贴出了调用 Ark Chat Completions 的 curl，请求里包含 system prompt、user prompt 和 `search_log` 工具定义。这个工具的 parameters 是标准 JSON Schema，包含 `psm`、`log_id`、`key_words`、`start_time`、`end_time`、`location` 等字段，并且设置了 required 和 `additionalProperties: false`。王子健还贴图指出 schema 里确实有 `properties`。王德政判断这是 JSON Schema，输入本身看起来没有明显问题。

王德政给出的后续建议是：如果输入没有问题，可以尝试换模型、调整 prompt，或者拿 logid 去找 Ark 侧排查。也就是说，当前现象可能不是 workflow agent 不支持工具调用，而是模型生成参数、schema 适配或下游序列化链路中的某个环节有问题，需要结合真实请求和模型侧日志进一步看。

后半段王子健又追问子 agent 的上下文和输入可见性。他问每次 agent 调用结果是否都会放到上下文里，以及哪里能看到对应上下文。王德政回答说上下文会作为每个子 agent 的输入，但完整上下文在外部看不到；子 agent 自己可以看到自己的输入。王德政进一步说明可以看 `adk.Agent` 接口，里面有 `run` 方法；王子健使用的 chat model agent 是框架提供的封装。

群尾部马荣鸿询问是否还有其他问题，王子健请求先帮忙保存，马荣鸿执行 `/save` 后系统确认该群已保存为长期关注，不会被解散，后续可继续回到该群跟进。整体来看，这个群的结论是：Workflow Agent 本身支持工具调用；当 arguments 已生成但工具请求结构体为空时，应优先查看真实模型请求、工具 JSON Schema、泛型或反序列化类型是否匹配，并在输入看起来正常时进一步排查模型输出质量或 Ark 侧日志；关于子 agent，上下文会作为输入传入，但外部封装下不一定能直接看到完整上下文。
